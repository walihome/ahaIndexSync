name: Run PROD

on:
  schedule:
    - cron: '0 18 * * *'  # 北京时间凌晨 2:00 (UTC+8)
  workflow_dispatch:

jobs:
  run:
    uses: ./.github/workflows/_pipeline.yml
    with:
      table_suffix: ""
      run_mode: "daily"
      notify_aha_index2: true
    secrets: inherit
